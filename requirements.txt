# Ambiente único do repositório — UNIMED SP · Simulação em Serviços de Saúde
# Testado com Python 3.14.2
#
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
#
# Os módulos também têm requisitos próprios, menores, para deploy isolado
# das aplicações: F1/streamlit/requirements.txt e D10/streamlit/requirements.txt

# --- base numérica e de dados (todos os módulos) ---
numpy==2.4.4
pandas==3.0.3
scipy==1.17.1
requests==2.34.0

# --- visualização ---
matplotlib==3.10.9
seaborn==0.13.2
plotly==6.7.0

# --- simulação de eventos discretos (D9, D10, E3, E4, F1) ---
simpy==4.1.1

# --- otimização prescritiva (D10) ---
pyomo==6.10.0
highspy==1.14.0

# --- séries temporais e previsão (F1) ---
statsmodels==0.14.6
prophet==1.3.0
holidays==0.96

# --- aprendizado de máquina (F1) ---
scikit-learn==1.9.0
xgboost==3.3.0
lightgbm==4.7.0
catboost==1.2.10

# --- aplicações interativas ---
streamlit==1.57.0

# --- notebooks ---
ipykernel==7.2.0
ipywidgets==8.1.8

# --- testes (F1/streamlit/testes) ---
pytest==9.1.1
